@AGENTS.md

# テストに関するルール

- テストを通すためにテストコード自体(仕様・期待値・アサーション)を変更してはいけない。テストが失敗した場合は、実装側を修正して仕様を満たすこと。
- テストの仕様変更が本当に必要だと判断した場合は、無断で変更せず、必ず理由を説明してユーザーの承認を得てから変更すること。

# 実装フローに関するルール

- 実装やテストを行う前に、関連するドキュメント(`node_modules/next/dist/docs/` 等)を確認してから着手すること。
- 実装が完了したら、`tasks.md` の内容(タスクの状態など)を必ず更新すること。
